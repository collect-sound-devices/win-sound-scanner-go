version: "2"

run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - gocyclo
    - cyclop

  settings:
    gocyclo:
      min-complexity: 11

    cyclop:
      max-complexity: 11

issues:
  exclude-use-default: false