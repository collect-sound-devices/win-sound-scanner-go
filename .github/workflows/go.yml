name: Go

on:
  push:
    branches:
      - main
    tags:
      - 'Release-*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  setup-and-compute-version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get_version.outputs.VERSION }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Unshallow repository
        run: git fetch --prune --unshallow

      - name: Determine version (SemVer)
        id: get_version
        shell: pwsh
        run: |
          $ref = $env:GITHUB_REF
          # Find the latest Release-* tag.
          $latestTag = git describe --tags --match "Release-*" --abbrev=0
          if (-not $latestTag) {
              Write-Host "No Release tag found. Using default version 1.0.0."
              "VERSION=1.0.0" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
              "version=1.0.0" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
              exit 0
          }
          # Extract version number from the tag (e.g., Release-1.0.1 -> 1.0.1)
          $versionNumber = $latestTag -replace '^Release-', ''
          Write-Host "latestTag: $latestTag, versionNumber: $versionNumber"
          $distance = git rev-list --count "$latestTag..HEAD"
  
          # Append -rc<distance> if distance is greater than 0
          if ($distance -gt 0) {
              # Increment the patch number for a prerelease build
              $versionParts = $versionNumber -split '\.'
              $versionParts[2] = [int]$versionParts[2] + 1
              $versionNumber = $versionParts -join '.'
              Write-Output "VERSION CORRECTED, versionNumber: $versionNumber"
              $version = "$versionNumber-rc.$distance"
          } else {
              $version = $versionNumber
          }
          $version = 'v' + $version
          Write-Host "Derived development version: $version (base tag: $latestTag)"
          "VERSION=$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          "version=$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          Write-Host "Using version: $version"

  build:
    runs-on: windows-latest
    needs: setup-and-compute-version
    permissions:
      contents: read
      packages: write
    env:
      APP_NAME: win-sound-scanner
      VERSION: ${{ needs.setup-and-compute-version.outputs.version }}
      WIN_SOUND_SIGN_PFX_BASE64: ${{ secrets.WIN_SOUND_SIGN_PFX_BASE64 }}
      WIN_SOUND_SIGN_PFX_PASSWORD: ${{ secrets.WIN_SOUND_SIGN_PFX_PASSWORD }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.x"

      - name: Set build version
        shell: powershell
        run: |
          $version = "${{ env.VERSION }}"
          if (-not $version) {
            $version = "unknown"
          }
          if ($version.StartsWith("v")) {
            $version = $version.Substring(1)
          }
          Write-Host "Computed APP_VERSION=$version"
          "APP_VERSION=$version" | Out-File -FilePath $Env:GITHUB_ENV -Encoding utf8 -Append

      - name: Restore signing certificate
        shell: powershell
        run: |
          if ([string]::IsNullOrWhiteSpace($Env:WIN_SOUND_SIGN_PFX_BASE64) -or [string]::IsNullOrWhiteSpace($Env:WIN_SOUND_SIGN_PFX_PASSWORD)) {
            Write-Host "Signing secrets are not configured. Continuing without signing."
            exit 0
          }

          $certPath = Join-Path $Env:RUNNER_TEMP "win-sound-scanner-codesign.pfx"
          [System.IO.File]::WriteAllBytes($certPath, [Convert]::FromBase64String($Env:WIN_SOUND_SIGN_PFX_BASE64))
          "WIN_SOUND_SIGN_PFX_PATH=$certPath" | Out-File -FilePath $Env:GITHUB_ENV -Encoding utf8 -Append
          "WIN_SOUND_SIGN_PFX_PASSWORD=$Env:WIN_SOUND_SIGN_PFX_PASSWORD" | Out-File -FilePath $Env:GITHUB_ENV -Encoding utf8 -Append

      - name: Build Go binary
        shell: powershell
        run: |
          .\scripts\build.ps1 -appName $Env:APP_NAME -appVersion $Env:APP_VERSION -respectExistingCompiler

      - name: Package bin directory
        shell: powershell
        run: |
          $artifactName = "WinSoundScanner-$Env:APP_VERSION.zip"
          Write-Host "Creating artifact $artifactName from bin/ ..."
          $binPath = Join-Path $PWD "bin"
          if (-not (Test-Path -Path $binPath)) {
            throw "bin directory not found; nothing to package."
          }
          $binGlob = Join-Path $binPath "*"
          Compress-Archive -Path $binGlob -DestinationPath (Join-Path $PWD $artifactName) -Force

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Artifacts
          path: WinSoundScanner-*.zip
          if-no-files-found: error

  publish:
    if: github.ref != 'refs/pull/develop'
    # if: startsWith(github.ref, 'refs/tags/v')
    needs:
      - setup-and-compute-version
      - build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: Artifacts
          path: build

      - name: Create or update Release
        env:
          VERS: ${{ needs.setup-and-compute-version.outputs.version }}
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | gh auth login --with-token
          if gh release view "$VERS" > /dev/null 2>&1; then
            echo "Release $VERS already exists; updating assets"
          else
            gh release create "$VERS" --title "WinSoundScanner $VERS" --notes "Release $VERS"
          fi
          # Upload or update the build artifact as a release asset
          if [ -f "build/WinSoundScanner-$VERS.zip" ]; then
            gh release upload "$VERS" "build/WinSoundScanner-$VERS.zip" --clobber
          elif [ -n "$(ls build 2>/dev/null)" ]; then
            # Fallback: upload any file(s) in the build directory
            gh release upload "$VERS" build/* --clobber
          else
            echo "No artifacts found in build/ to attach to release $VERS"
          fi
